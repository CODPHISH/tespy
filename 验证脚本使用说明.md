# Stage2 模型验证脚本使用说明

## 概述

本文档介绍如何使用 `validate_stage2_model.py` 验证脚本对 Stage2 详细模型进行验证。该脚本能够：

1. 验证模型独立收敛能力
2. 读取参考 CSV 数据并与当前仿真结果进行对比
3. 计算绝对/相对误差并生成统计信息
4. 输出中文验证报告（Markdown 和 JSON 格式）
5. 保留关键输出（组件功率、主蒸汽状态等）

## 快速开始

### 基础使用

直接运行验证脚本，使用默认参数：

```bash
python validate_stage2_model.py
```

默认行为：
- 参考数据路径：`boiler-turbine_design_state/`
- 输出目录：`validation_results/`

### 指定输出目录

将验证报告保存到自定义目录：

```bash
python validate_stage2_model.py --output-dir my_validation_results
```

### 指定参考数据路径

如果参考 CSV 数据位于其他位置：

```bash
python validate_stage2_model.py --reference-path /path/to/reference/data
```

### 完整参数示例

同时指定参考数据路径和输出目录：

```bash
python validate_stage2_model.py --reference-path ./boiler-turbine_design_state --output-dir ./results_20240101
```

## 输出文件说明

验证脚本运行后，会在输出目录生成以下文件：

### 1. 验证报告.json

包含完整的验证数据和中文字段描述，适合程序解析和进一步处理。

主要内容：
- 收敛指标（是否收敛、迭代次数、组件数量等）
- 关键输出（主蒸汽状态、功率汇总、汽轮机分段功率）
- 对比结果（连接对比、组件对比、汇总统计）
- 误差统计（各参数的绝对/相对误差统计）
- 容差设定（质量流量、压力、温度、焓、功率的容差）
- 验证结论（一致性评价、总体评价）
- 模型输入输出对应关系

### 2. 验证报告.md

中文详细报告，包含图表和说明，适合人工阅读和存档。

报告章节：
- 概要总结
- 收敛指标
- 对比汇总
- 容差设定
- 关键输出
- 误差统计
- 超出容差的连接
- 缺失连接
- 汽轮机功率对比
- 模型输入输出对应关系
- 验证结论
- 参考数据来源
- 使用说明

### 3. REPAIR_AND_VALIDATION_ANALYSIS.md

技术分析报告，描述模型修复思路、验证结果和使用建议。

## 参数对比说明

### 容差设定

验证脚本使用以下容差判断参数是否匹配：

| 参数 | 绝对容差 | 相对容差 | 单位 |
|------|----------|----------|------|
| 质量流量 | ±0.1 | ±1% | t/h |
| 压力 | ±0.5 | ±1% | bar |
| 温度 | ±2.0 | ±1% | °C |
| 焓 | ±5.0 | ±1% | kJ/kg |
| 功率 | ±0.1 | ±2% | MW |

只要满足绝对容差**或**相对容差之一，即认为该参数在容差范围内。

### 对比范围

脚本会对比以下内容：

#### 连接状态
- 质量流量 (m)
- 压力 (p)
- 温度 (T)
- 焓 (h)

#### 组件参数
- 汽轮机功率 (P)
- 换热器传热量 (Q)

## 验证流程

验证脚本按以下步骤执行：

1. **加载参考数据**
   - 读取 `connections.csv`
   - 读取 `components/*.csv`

2. **构建和求解模型**
   - 使用 `stage2_detailed_model.py` 构建模型
   - 在不依赖导出状态的情况下求解（验证独立收敛能力）
   - 收集收敛指标（迭代次数、残差等）

3. **收集关键输出**
   - 主蒸汽状态
   - 烟气出口状态
   - 汽轮机各段功率
   - 泵功率
   - 净功率

4. **与参考数据对比**
   - 逐个连接对比状态参数
   - 对比组件功率
   - 计算绝对和相对误差

5. **生成统计和报告**
   - 计算匹配率
   - 统计误差分布
   - 生成验证结论
   - 输出报告文件

## 验证结论判定标准

根据匹配率（在容差内的连接比例），脚本会给出以下评价：

- **优秀**：匹配率 ≥ 95%
- **良好**：85% ≤ 匹配率 < 95%
- **中等**：70% ≤ 匹配率 < 85%（建议检查差异较大的连接）
- **较差**：匹配率 < 70%（需要进一步检查模型设置）

总体通过标准：
- ✅ **通过**：模型收敛 且 匹配率 ≥ 85%
- ⚠️ **有条件通过**：模型收敛 且 70% ≤ 匹配率 < 85%
- ⚠️ **需改进**：模型收敛 但 匹配率 < 70%
- ❌ **未通过**：模型未收敛

## 常见问题

### Q1: 如何解读误差统计？

误差统计提供了各参数的详细误差分布：
- **平均绝对误差**：所有对比点的绝对差异平均值
- **最大绝对误差**：所有对比点中的最大差异
- **平均相对误差**：所有对比点的相对差异平均值（百分比）
- **最大相对误差**：所有对比点中的最大相对差异（百分比）
- **样本数量**：参与对比的连接数
- **超差数量**：超出容差范围的连接数

### Q2: 为什么某些连接标记为"缺失"？

可能的原因：
- 参考 CSV 中存在该连接，但当前模型中未定义
- 连接名称不匹配（检查标签是否一致）

### Q3: 如何提高匹配率？

1. 检查边界条件设置是否与参考 CSV 一致
2. 确认流体组分配置正确
3. 验证组件参数（效率、压比、传热系数等）是否正确读取
4. 对于超差较大的连接，单独分析其上下游组件设置

### Q4: 可以修改容差设置吗？

可以。在 `validate_stage2_model.py` 中修改 `Stage2ModelValidator` 类的 `__init__` 方法中的 `self.tolerances` 字典。

## 集成到开发流程

### 建议的验证流程

1. **模型开发/修改后立即验证**
   ```bash
   python validate_stage2_model.py --output-dir results_dev
   ```

2. **定期回归验证**（每周或每个版本发布前）
   ```bash
   python validate_stage2_model.py --output-dir results_$(date +%Y%m%d)
   ```

3. **CI/CD 集成**
   在 CI 流程中添加验证步骤，自动检查模型一致性：
   ```yaml
   - name: Validate Stage2 Model
     run: python validate_stage2_model.py --output-dir ci_validation
   ```

## 技术支持

如遇到问题或需要帮助，请：
1. 查看生成的验证报告中的详细错误信息
2. 检查模型日志（如果有）
3. 参考 `REPAIR_AND_VALIDATION_ANALYSIS.md` 中的技术分析

## 更新日志

- 初始版本：支持基本验证功能和中文报告生成
- 当前版本：增强误差统计、关键输出采集、中文化报告

---

*最后更新：2024*
